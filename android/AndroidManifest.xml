<?xml version="1.0" encoding="utf-8"?>
<!-- Velix для Android: оболочка вокруг того же веб-клиента, что раздаёт сервер -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.vexorter.velix"
          android:versionCode="22"
          android:versionName="0.2.9.8">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Приложение обновляет себя само: систему всё равно спросят,
         ставить ли, но без этого разрешения даже спросить нельзя -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Служба со связью живёт, пока чат свёрнут: без этого Android
         усыпил бы сокет и уведомления не приходили бы -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Голосовые и кружочки: без них записывать нечем -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera"
                  android:required="false" />
    <uses-feature android:name="android.hardware.microphone"
                  android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".VelixService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
