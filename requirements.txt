# Начиная с 14.0 обработчик подключения принимает один аргумент (websocket),
# в более старых версиях — два (websocket, path). Код рассчитан на новый API.
websockets>=14,<16

# Для истории сообщений используется sqlite3 — он входит в стандартную
# библиотеку Python, ставить отдельно ничего не нужно.

# Только для оконного клиента gui.py; серверу и client.py не нужен.
customtkinter>=6,<7
