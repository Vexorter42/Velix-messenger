# Начиная с 14.0 обработчик подключения принимает один аргумент (websocket),
# в более старых версиях — два (websocket, path). Код рассчитан на новый API.
websockets>=14,<16

# Для истории сообщений используется sqlite3 — он входит в стандартную
# библиотеку Python, ставить отдельно ничего не нужно.

# Только для оконного клиента gui.py; серверу и client.py не нужен.
customtkinter>=6,<7

# Pillow нужен и оконному клиенту (показывает картинки и крутит гифки),
# и серверу (сжимает вложения). customtkinter его в зависимостях не
# объявляет, хотя использует.
Pillow>=10

# Значок в области уведомлений. Без него клиент работает, просто
# закрытие окна будет означать выход.
pystray>=0.19

# Уведомления на телефон, когда приложение закрыто. Нужен только
# серверу; без него чат работает, просто молча.
pywebpush>=2
